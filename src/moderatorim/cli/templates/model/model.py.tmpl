"""The {domain} domain's model."""

from __future__ import annotations

from moderatorim.sdk import Field, FieldType, Model


class {Domain}(Model):
    table = "{app}_{domain}"
    fields = {{
{fields}
    }}
