"""The {domain} domain's model."""

from __future__ import annotations

from moderatorim.sdk import FieldType, TableColumn, TableModel

{Domain} = TableModel(
    table="{app}_{domain}",
    columns=(
{fields}
    ),
)
