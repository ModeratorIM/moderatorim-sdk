"""The {domain} domain's view / render helpers (build HTML strings for Page.content)."""

from __future__ import annotations


def {domain}_screen() -> str:
    """Render the {domain} screen as an HTML string."""
    return "<h1>{Domain}</h1>"
