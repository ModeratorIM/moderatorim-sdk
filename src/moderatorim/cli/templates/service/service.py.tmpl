"""The {domain} domain's service — business logic behind the routes."""

from __future__ import annotations

from typing import Any


class {Domain}Service:
    """Operations for the {domain} domain. Reaches core only through injected handles."""

    def __init__(self, store: Any) -> None:
        self.store = store
