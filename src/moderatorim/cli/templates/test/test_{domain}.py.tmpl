"""Tests for the {domain} domain. Fill in and run with pytest."""

from __future__ import annotations


def test_{domain}_placeholder() -> None:
    # TODO: write real assertions for the {domain} domain.
    assert True
